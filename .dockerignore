# Ignore everything, then whitelist what the build needs.
# Keeps the Docker context small and the build cache stable.

*

!cmd/
!internal/
!migrations/
!frontend/
frontend/node_modules/
frontend/dist/
!go.mod
!go.sum
